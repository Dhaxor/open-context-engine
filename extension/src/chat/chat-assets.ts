// Deprecated. The chat webview now lives in compiled modules under ./webview/* (built to
// dist/webview.{js,css}) plus the static body in ./chat-html.ts. Kept as an empty module
// for backwards compatibility; safe to delete.
export {};
